---
name: testing

on:
  workflow_dispatch:

jobs:
  Setup:
    uses: ./.github/workflows/getNewVersion.yml
    with:
      image: testdevops
    secrets:
      account:  ${{ secrets.GCP_DEV_ACC }}
      GCPCreds: ${{ secrets.GCP_DEV }} 
      

  testit:
    runs-on:  ubuntu-latest
    needs: Setup

    steps:
      - run: echo ${{ needs.Setup.outputs.version }}
