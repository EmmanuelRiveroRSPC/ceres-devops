---
name: testing if

on:
  workflow_call:
    inputs:
      testout:
        required: false
        type: boolean
        default: false
      
      
jobs:
  test1:
    name: test 1
    runs-on: ubuntu-latest

    outputs:
      version: ${{ steps.version.outputs.gcloud_version }}
  
    steps:
      - if: ${{ !inputs.testout }}
        run: echo run1 ${{inputs.testout}}
      
      - if: ${{ inputs.testout }}
        run: echo run2 ${{inputs.testout}}

      - run: echo run3